apiVersion: v1
kind: Service
metadata:
  name: {{ include "hugginghack.fullname" . }}
  labels:
    {{- include "hugginghack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "hugginghack.selectorLabels" . | nindent 4 }}
